# ⚠️ Not in use

   This is an unused React/Vite scaffold — an earlier idea for a rewrite that was never finished.

   The live app is the root-level `clearlens.html` + `/api` functions. This folder is not built, not deployed, and not referenced by `vercel.json`.

   Safe to ignore. Will either be finished or deleted later.